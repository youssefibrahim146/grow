class GetStartedImageModel {
  int index;
  String title, image;

  double opacity, position;

  GetStartedImageModel(this.title, {required this.index, required this.image, required this.opacity, required this.position});
}
